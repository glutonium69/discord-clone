export default function ServerIcons({icon}) {

    return (
        <div>
            <a href="#" className="server-icon">
                <img src={icon} alt="" />
            </a>
        </div>
    );
}